# Template Mapping Guardrail

Template ingredient mapping resolves template text to canonical ingredient identity only.

It must not:
- choose inventory items by shortcut
- choose vendor items by shortcut
- treat template ingredient text as operational stock identity

Template mapping is the first semantic bridge, not the final operational mapping layer.
